# BHPC Rich New Page Build Contract — Spry

BHPC agent recommendations must become semantic implementation blocks, framework/protocol/comparison/answer surfaces, or exact intended-winner repairs. Marker-only pages and generic thin pages are invalid.

Required validator:

```bash
npm run validate:bhpc-rich-new-page-contract
```
