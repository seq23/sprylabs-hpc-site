# Content Ownership and Lane Precedence

Ownership classes are `paid_agent`, `zero_dollar`, `legacy_eligible`, and `system_core`. Paid-agent and system-core routes are protected. The `$0` lane fills gaps and may mutate only its own or Safe-Harbor-eligible legacy routes.
