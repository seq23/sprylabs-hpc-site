# Hybrid Citation Engine

The paid weekly lane produces and owns its own pages. The daily `$0` lane subtracts that coverage, then fills uncovered AEO, SEO, GEO, source, freshness, answer, entity, and graph gaps across eligible non-agent content. External outcomes are never claimed without telemetry.
