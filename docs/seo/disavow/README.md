# Billionaire High Performance Coach Disavow Asset

## Repository scope

This repository serves `billionairehighperformancecoach.com` and `spryexecutiveos.com`. The supplied disavow package contained one shared file for `aplayermode.com` and `billionairehighperformancecoach.com`. The relevant domain list is versioned here as:

`docs/seo/disavow/billionairehighperformancecoach.com-disavow.txt`

The Industry Guides disavow file was not copied into this repository because it belongs to a different property and repository.

## Important boundary

A repository change cannot submit a disavow file to Google Search Console. Upload remains a manual property-level action. This delivery versions and validates the exact text asset; it does not claim that Google has received it.

## Manual upload

1. Open the Google Search Console Disavow Links tool.
2. Select the `billionairehighperformancecoach.com` property.
3. Upload `billionairehighperformancecoach.com-disavow.txt`.
4. Confirm the upload.
5. Save the Search Console confirmation outside the repository.

Do not upload this file to `spryexecutiveos.com`; the supplied package explicitly excluded that property.

## Validation

The file must:

- contain only comments, blank lines, and `domain:` directives;
- contain no duplicate domains;
- contain exactly 14 domain directives;
- contain no self-domain directive for `billionairehighperformancecoach.com` or `spryexecutiveos.com`;
- remain UTF-8 plain text.

The source package's embedded comment states `Generated: 2026-11-19`. That metadata is preserved verbatim from the supplied asset and was not independently verified by this repository change.
