/help-me-get-my-life-together/ /how-to-get-your-life-together/ 301
/insights/discipline-vs-motivation-high-performance.html /discipline-vs-motivation/ 301
/insights/discipline-vs-motivation-the-calm-version-that-works.html /discipline-vs-motivation/ 301
# The clean-URL forms of the same two retired pages. Since canonical routes
# dropped .html, an inbound link or a stale index can name either shape.
/insights/discipline-vs-motivation-high-performance /discipline-vs-motivation/ 301
/insights/discipline-vs-motivation-the-calm-version-that-works /discipline-vs-motivation/ 301

/admin.html /admin/ 301

# Recover stale or previously leaked internal source URLs.
/site/public/* /:splat 301

# Canonicalize legacy HTML URLs before the repository-root compatibility proxy.
# Without this rule, Pages can expose /site/public during its automatic clean-URL redirect.
/*.html /:splat 301

# /coverage/ was renamed to /knowledge-map/. The old name collided with the Node
# convention for test-coverage output, which is why the directory sat in
# .gitignore and the page 404'd for two months while the sitemap advertised it.
/coverage/ /knowledge-map/ 301
/coverage /knowledge-map/ 301
/coverage.json /knowledge-map.json 301

# Retired 2026-08-27: 743 `bhpc-daily-citation-gap-fill-*` pages and the 10
# paginated `chatgpt-prompts-and-setup-N` hubs that existed only to carry links
# to them. The pages were produced by a quota branch that manufactured filler
# when an agent report supplied fewer pages than a 75/day target, and two pages
# on different topics were 97.83% word-identical. Cause fixed in 87ecb6780.
#
# Wildcards rather than 753 explicit rules: each source needs both its .html and
# extensionless form, so explicit rules would approach the 2,100-rule ceiling for
# no benefit. Neither pattern can over-match - the surviving canonical hub is
# `/insights/topics/chatgpt-prompts-and-setup/` with no dash suffix, and no file
# beginning `bhpc-daily-citation-gap-fill` remains on disk.
/insights/bhpc-daily-citation-gap-fill-* /insights/topics/chatgpt-prompts-and-setup/ 301
/insights/topics/chatgpt-prompts-and-setup-* /insights/topics/chatgpt-prompts-and-setup/ 301
