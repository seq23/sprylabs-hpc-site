#!/usr/bin/env node
import { readJson, writeJson, stamp, OK, UNAVAILABLE } from './lib/core.mjs';
const contract=readJson('data/search_intelligence/search_intelligence_contract.json',{});const own=new Set(contract.public_domains||[]);const o=readJson('data/search_intelligence/live_search_observations.json',{provider_state:UNAVAILABLE,observations:[]});
const observed=(o.observations||[]).filter(x=>x.status==='OBSERVED');const per_query=[];const byDomain=new Map();
for(const x of observed){const domains=[...new Set((x.referenced_domains||[]).filter(d=>!own.has(String(d).replace(/^www\./,''))))].sort();for(const d of domains){const v=byDomain.get(d)||{domain:d,observed_reference_count:0,target_ids:[]};v.observed_reference_count++;v.target_ids.push(x.target_id);byDomain.set(d,v)}per_query.push({target_id:x.target_id,query:x.query,own_domain_referenced:x.own_domain_referenced===true,competitor_domains:domains,competitor_domain_count:domains.length,comparison_verdict:x.own_domain_referenced===true?(domains.length?'OWN_AND_COMPETITORS_REFERENCED':'ONLY_OWN_REFERENCED'):(domains.length?'ONLY_COMPETITORS_REFERENCED':'NOTHING_REFERENCED'),evidence_ref:x.evidence_ref,truth_boundary:'Referenced-domain comparison from grounded search; not a ranking.'})}
const state=o.provider_state===OK&&observed.length?OK:observed.length?'DEGRADED':UNAVAILABLE;writeJson('data/search_intelligence/competitor_comparison.json',{schema_version:'1.1',generated_at:stamp(),provider_state:state,overall_status:state,status_is_healthy:state===OK,observed_query_count:observed.length,competitors:[...byDomain.values()].map(x=>({...x,target_ids:[...new Set(x.target_ids)]})).sort((a,b)=>b.observed_reference_count-a.observed_reference_count||a.domain.localeCompare(b.domain)),per_query,unavailable_note:observed.length?null:'No successful grounded observations; no competitor claims produced.'});console.log(`[search:competitors] ${state} queries=${observed.length} domains=${byDomain.size}`);
