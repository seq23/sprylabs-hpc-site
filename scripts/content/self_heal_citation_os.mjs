#!/usr/bin/env node
import fs from 'node:fs';import {readJson,writeJson,unsafeClaim,rewriteUnsafe,now} from '../lib/safe_harbor_utils.mjs';
const app=readJson('artifacts/validation/release-plan-application.json',{applied:[]});const rewrites=readJson('data/governance/safe_harbor_rewrite_ledger.json',{schema_version:'1.0',entries:[]});let repaired=0;for(const x of app.applied||[]){if(!fs.existsSync(x.source_file))continue;const before=fs.readFileSync(x.source_file,'utf8');if(!unsafeClaim(before))continue;const after=rewriteUnsafe(before);if(after!==before){fs.writeFileSync(x.source_file,after);rewrites.entries.push({timestamp:now(),route:x.route_owner,source_file:x.source_file,decision:'REWRITTEN_AND_AUTOPUBLISHED'});repaired++;}}writeJson('data/governance/safe_harbor_rewrite_ledger.json',rewrites);console.log(`[citation:self-heal] PASS repaired=${repaired}`);
