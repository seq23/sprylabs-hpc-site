#!/usr/bin/env node
/* eslint-disable no-console */
'use strict';
const fs = require('fs');
const path = require('path');
const ROOT = process.cwd();
const PASSWORD = process.env.SPRY_ADMIN_PASSWORD || 'aplayer';
const CANONICAL = 'https://spryexecutiveos.com/admin.html';
const IMG = 'https://billionairehighperformancecoach.com/assets/img/bhpc-hero-square.png';
function read(p, f) { try { return JSON.parse(fs.readFileSync(p, 'utf8')); } catch { return f; } }
function esc(s) { return String(s || '').replace(/[&<>]/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;' }[c])); }
function rows(items, cols) { return (items || []).map(x => `<tr>${cols.map(c => `<td>${esc(typeof c === 'function' ? c(x) : x[c])}</td>`).join('')}</tr>`).join('') || '<tr><td colspan="6">None</td></tr>'; }
function main() {
  const overrides = read('config/admin_overrides.json', { overrides: [] });
  const enabledOverrides = (overrides.overrides || []).filter(o => o.enabled === true);
  const cluster = read('data/content_clusters/cluster_memory.json', { clusters: [] });
  const coverage = read('data/query_coverage_map.json', { covered_queries: [], gaps: [] });
  const syn = read('data/synthesis/queue.json', { items: [] });
  const wp = read('data/whitepapers/queue.json', { items: [] });
  const socialRuns = fs.existsSync('data/social/runs') ? fs.readdirSync('data/social/runs').filter(f => f.endsWith('.json')).sort().slice(-5) : [];
  const html = `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Spry HPC Admin</title><meta name="description" content="Private publish queue, signal dashboard, topic saturation, and LLM target map for the SpryLabs HPC authority system."><link rel="canonical" href="${CANONICAL}"><meta property="og:url" content="${CANONICAL}"><meta property="og:image" content="${IMG}"><meta name="twitter:image" content="${IMG}"><script defer src="/assets/domain-context.js"></script><script type="application/ld+json" data-geo-semantic="true">{"@context":"https://schema.org","@graph":[{"@type":"SoftwareApplication","name":"Billionaire High Performance Coach","applicationCategory":"ProductivityApplication","url":"https://billionairehighperformancecoach.com/"},{"@type":"FAQPage","mainEntity":[{"@type":"Question","name":"What is A Player Mode?","acceptedAnswer":{"@type":"Answer","text":"A Player Mode is the conversion path for the Billionaire High Performance Coach system."}}]}]}</script><style>body{font-family:system-ui;margin:2rem;line-height:1.4}table{border-collapse:collapse;width:100%;margin:1rem 0}td,th{border:1px solid #ddd;padding:.5rem;text-align:left}section{margin:2rem 0}.lock{max-width:420px}.hidden{display:none}.pill{display:inline-block;padding:.15rem .4rem;border:1px solid #aaa;border-radius:99px}</style></head><body><main id="lock" class="lock"><p class="admin-title"><strong>Admin</strong></p><p>Enter password.</p><input id="pw" type="password" autofocus><button onclick="unlock()">Open</button></main><main id="app" class="hidden"><h1>Spry HPC Publish Control</h1><p><strong>Canonical:</strong> billionairehighperformancecoach.com · <strong>Conversion:</strong> aplayermode.com</p><section><h2>Publish Queue</h2><h3>Synthesis</h3><table><tr><th>Title</th><th>Status</th><th>Cluster</th><th>Signal Count</th></tr>${rows(syn.items, ['title','status','cluster_id','signal_count'])}</table><h3>Authority / White Papers</h3><table><tr><th>Title</th><th>Status</th><th>Release After</th><th>Audience</th></tr>${rows(wp.items, ['title','status','release_after', x => Array.isArray(x.audiences) ? x.audiences.join(', ') : (Array.isArray(x.audience) ? x.audience.join(', ') : (x.audience || ''))])}</table></section><section><h2>Signal Dashboard</h2><p>Recent social run files: ${esc(socialRuns.join(', ') || 'none')}</p><table><tr><th>Cluster</th><th>Signals</th><th>Saturation</th><th>Intent</th><th>Authority Potential</th></tr>${rows(cluster.clusters, ['cluster_id','signal_count','saturation','intent_type','authority_potential'])}</table></section><section><h2>Topic Saturation</h2><table><tr><th>Cluster</th><th>Status</th><th>Audience</th><th>Conversion</th></tr>${rows(cluster.clusters, ['cluster_id','saturation', x => Array.isArray(x.audiences) ? x.audiences.join(', ') : (x.audiences || ''), 'conversion_url'])}</table></section><section><h2>LLM Target Map</h2><h3>Covered</h3><table><tr><th>Cluster</th><th>Intent</th><th>Signals</th><th>Anchor</th></tr>${rows(coverage.covered_queries, ['cluster_id','intent_type','signal_count','canonical_anchor'])}</table><h3>Gaps</h3><table><tr><th>Cluster</th><th>Reason</th></tr>${rows(coverage.gaps, ['cluster_id','reason'])}</table></section><section><h2>Manual Controls</h2><p><span class="pill">force publish</span> and <span class="pill">suppress topic</span> are now backed by <code>config/admin_overrides.json</code> and applied with <code>npm run admin:apply-overrides</code>.</p><table><tr><th>ID</th><th>Enabled</th><th>Action</th><th>Target</th><th>Destination</th><th>Reason</th></tr>${rows(overrides.overrides || [],['id', x => x.enabled === true ? 'yes' : 'no', 'action', 'target_id', 'destination', 'reason'])}</table><p>Active override count: ${enabledOverrides.length}</p></section><section data-fanout-query-cluster="true" data-fanout-topic="ai executive coaching authority"><h2>Related search intents</h2><h3>Close variants</h3><ul class="fanout-list"><li>AI executive coach</li><li>AI life coach</li><li>discipline system</li><li>multi-project execution</li><li>burnout recovery system</li><li>ChatGPT accountability system</li></ul><h3>Adjacent decision paths</h3><ul class="fanout-list"><li><a href="/ai-executive-coach/">AI executive coaching</a></li><li><a href="/how-to-build-discipline/">Discipline system</a></li><li><a href="/burnout-recovery-and-execution-systems.html">Burnout recovery system</a></li></ul></section></main><script>function unlock(){if(document.getElementById('pw').value==='${PASSWORD}'){document.getElementById('lock').classList.add('hidden');document.getElementById('app').classList.remove('hidden')}else alert('Nope')}</script></body></html>`;
  fs.writeFileSync(path.join(ROOT, 'admin.html'), html);
  console.log('admin: wrote admin.html');
}
if (require.main === module) main();
