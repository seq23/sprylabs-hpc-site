#!/usr/bin/env node
'use strict';const fs=require('fs');const path=require('path');function read(p,f={}){try{return JSON.parse(fs.readFileSync(p,'utf8'))}catch{return f}}function esc(s){return String(s??'Not available').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function card(k,v,n=''){return `<article class="card"><span>${esc(k)}</span><strong>${esc(v)}</strong>${n?`<small>${esc(n)}</small>`:''}</article>`}const g=read('data/admin/growth_health.json');const coverageOps=read('data/admin/coverage_operations.json',{totals:{},runway:{},clusters:[]});const a=read('data/governance/safe_harbor_audit_ledger.json',{entries:[]});const actions=read('data/admin/admin_action_registry.json',{actions:[]});const recent=(a.entries||[]).slice(-20).reverse();const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><meta name="description" content="Private Spry authority operations command center."><title>Spry Authority Operations</title><link rel="stylesheet" href="/assets/admin/admin.css"></head><body><div id="login" class="login"><h2>Spry Authority Operations</h2><p>Private command center. Routine content runs automatically.</p><form id="login-form"><input id="password" type="password" autocomplete="current-password" placeholder="Admin password"><button>Open command center</button></form><p id="login-status"></p></div><main id="app" hidden><header><div><p class="eyebrow">FULL SAFE AUTONOMY</p><h1>Authority Operations Command Center</h1><p>Visibility, diagnostics, configuration, and emergency control—not an approval queue.</p></div><button id="logout">Log out</button></header><section><h2>Executive health</h2><div class="grid">${card('100K progress',Math.round((g.goal?.progress_ratio||0)*100)+'%',`${g.goal?.owned_surfaces||0} owned surfaces`)}${card('Latest paid run',g.paid_agent?.latest_run)}${card('Paid-owned pages',g.paid_agent?.owned_pages||0)}${card('$0-owned pages',g.zero_dollar?.owned_pages||0)}${card('Latest $0 pages',g.zero_dollar?.latest_applied||0)}${card('Skipped safely',g.exceptions?.total||0)}</div></section><section><h2>Paid weekly agent</h2><p>Artifacts: ${esc((g.paid_agent?.artifacts||[]).join(', '))}</p><p>Accepted agent records: <strong>${esc(g.paid_agent?.accepted_entries||0)}</strong>. This lane owns and maintains its pages.</p></section><section><h2>Daily $0 gap-filling lane</h2><p>Publishes inside Safe Harbor, excludes paid-agent pages, and continues after isolated skips.</p><div class="grid">${card('Latest applied',g.zero_dollar?.latest_applied||0)}${card('Latest skipped',g.zero_dollar?.latest_skipped||0)}${card('Legacy eligible',g.authority?.legacy_pages||0)}${card('System core',g.authority?.system_core||0)}</div></section><section><h2>Private content runway</h2><p>Operational draft inventory stays here and is not published on the public coverage map.</p><div class="grid">${card('Published pages',coverageOps.totals?.live||0)}${card('Draft inventory',coverageOps.totals?.drafts||0)}${card('Tracked clusters',coverageOps.totals?.clusters||0)}${card('Runway end',coverageOps.runway?.maxDraftDate||'No dated drafts')}</div><p class="small">Next dated releases: ${esc((coverageOps.runway?.next10DraftDates||[]).join(', ')||'None scheduled')}</p></section><section><h2>Outcomes</h2><div class="grid">${card('Observed LLM mentions',g.outcomes?.observed_llm_mentions,'Not claimed until measured')}${card('Indexed URLs',g.outcomes?.indexed_urls,'Requires provider data')}${card('/download referrals',g.outcomes?.download_referrals,'Requires analytics')}${card('Gumroad sales',g.outcomes?.gumroad_sales,'Requires attribution')}</div></section><section><h2>Safe controls</h2><div id="actions" class="actions">${(actions.actions||[]).map(x=>`<button class="action-card" data-action="${esc(x.id)}" data-confirm="${esc(x.confirmation||('Run '+x.label+'?'))}"><span class="impact ${x.changes_public_site?'live':''}">${x.changes_public_site?'May change live site':'Control or distribution action'}</span><strong>${esc(x.label)}</strong><small>${esc(x.description||'Registered operation')}</small></button>`).join('')}</div><p id="action-status" class="status"></p><div id="action-result" class="result" hidden></div></section><section><h2>Recent autonomous decisions</h2><table><thead><tr><th>Time</th><th>Decision</th><th>Route</th><th>Reason</th></tr></thead><tbody>${recent.map(x=>`<tr><td>${esc(x.timestamp)}</td><td>${esc(x.decision)}</td><td>${esc(x.route)}</td><td>${esc(x.reason)}</td></tr>`).join('')||'<tr><td colspan="4">No decisions recorded yet.</td></tr>'}</tbody></table></section></main><script src="/assets/admin/admin.js" defer></script></body></html>`;fs.mkdirSync('admin',{recursive:true});fs.writeFileSync('admin/index.html',html);fs.writeFileSync('admin.html','<!doctype html><html><head><meta charset="utf-8"><meta name="robots" content="noindex,nofollow"><meta name="description" content="Redirect to the private Spry authority operations command center."><meta http-equiv="refresh" content="0;url=/admin/"><title>Opening Spry Admin</title></head><body><p><a href="/admin/">Open admin</a></p></body></html>');console.log('admin:build PASS');
