One shared layout that guarantees:

CSS always loads

Header + nav always render correctly

CTA bar always present

Atlas sub-nav never clips

Generated content drops into a safe container

Google + LLM-friendly semantics

You apply this once, then all generators inherit it.
