PHASE 32 — INSIGHTS INDEX CARDS + AUTOPUBLISH (NO PLACEHOLDER DRIFT)
Base: MODEL_AUTHORITY_LAYER_PHASE30_HOME_ATLAS_NAV_DOMINANCE_CLEAN_AUDIT_FOLDER.zip
Date: 2026-02-15

Objective
- Insights index must remain populated with "nice cards" after running the Daily Insights Publish workflow.
- New insights must auto-appear on /insights/ after each publish.
- No template placeholders (e.g., {{search_block}}) should ever ship to production HTML.

Changes (code-level)
1) scripts/build_insights.js
   - renderPage(): added replacements for:
     - {{canonical_url}} (back-compat)
     - {{search_block}} => "" (empty)
     - {{related_block}} => "" (empty)
   - buildInsightsIndex(): switched from UL list to card grid using .card-grid/.card.
     Includes: title link + description (if present) + date + cluster label.

2) templates/layout.html
   - canonical/og:url placeholders normalized to {{canonical}}.
   - footer checkout turned into a clickable Gumroad link.
   - removed hardcoded FAQPage JSON-LD; replaced with {{json_ld}} so builders control schema.

Verification (performed)
A) Ran: node scripts/build_insights.js
   Result: success (no errors).

B) Post-build checks
   - /insights/index.html contains: <div class="card-grid"> ✅
   - /insights/index.html contains NO placeholders:
     - {{search_block}} ✅ (absent)
     - {{related_block}} ✅ (absent)
     - {{canonical_url}} ✅ (absent)

C) Drift protection
   - Daily workflow step "Build insights + topics + sitemap" calls scripts/build_insights.js.
   - Since index is now generated as card-grid and placeholders are hard-cleared in renderPage, future publishes cannot regress into raw template tokens.

Expected outcome in production
- /insights/ shows a card list of all insights (existing + future).
- After next publish, the new insight appears automatically on /insights/ without manual edits.
