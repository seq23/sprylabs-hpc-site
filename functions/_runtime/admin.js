const enc=new TextEncoder();function b64(bytes){return btoa(String.fromCharCode(...bytes)).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}function unb64(s){s=s.replace(/-/g,'+').replace(/_/g,'/');return Uint8Array.from(atob(s),c=>c.charCodeAt(0))}async function sign(payload,secret){const key=await crypto.subtle.importKey('raw',enc.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['sign']);return b64(new Uint8Array(await crypto.subtle.sign('HMAC',key,enc.encode(payload))))}export async function createSession(secret){const payload=b64(enc.encode(JSON.stringify({exp:Date.now()+8*60*60*1000})));return `${payload}.${await sign(payload,secret)}`}export async function validSession(request,secret){const m=(request.headers.get('cookie')||'').match(/spry_admin=([^;]+)/);if(!m)return false;const [p,s]=m[1].split('.');if(!p||!s||await sign(p,secret)!==s)return false;try{return JSON.parse(new TextDecoder().decode(unb64(p))).exp>Date.now()}catch{return false}}export function json(value,status=200,headers={}){return new Response(JSON.stringify(value),{status,headers:{'content-type':'application/json','cache-control':'no-store',...headers}})}
